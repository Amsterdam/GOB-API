pool:
  vmImage: ubuntu-latest

variables:
  VERSION_TAG: '0.12.0'
  # containerRegistry: 'Container Registry - piosupportacr'
  # containerRegistryFullName: 'piosupportacr.azurecr.io'
  # repository: 't-rex'

resources:
  repositories:
  - repository: Gob-API
    type: github
    endpoint: Amsterdam
    name: Amsterdam/Gob-API


steps:
- checkout: self

- task: CmdLine@2
  displayName: initiatePipelineVars
  inputs:
    script: |
      echo "##vso[task.setvariable variable=timeStamp]$(date +%y%m%d%H%M%S)"
      echo "##vso[task.setvariable variable=productName]$(echo ${SYSTEM_DEFINITIONNAME}| tr '[:upper:]' '[:lower:]')"
      echo "##vso[task.setvariable variable=productRepo]${SYSTEM_DEFINITIONNAME}"
      #echo $(productRepo)-$(productName)
      #workingDirectory: # Optional
      #failOnStderr: false # Optional

- task: CmdLine@2
  displayName: printenv
  inputs:
    script: |
      printenv



# - task: Docker@2
#   displayName: $(productName)BuildPush
#   inputs:
#     containerRegistry: 'Container Registry - piosupportacr'
#     repository: 'Gob-API'
#     command: 'buildAndPush'
#     # arguments: --build-arg VERSION_TAG='$(VERSION_TAG)'
#     Dockerfile: 'Gob-API/src/Dockerfile'
#     tags: |
#       $(Build.BuildId)-$(timeStamp)
#       latest

# - task: Docker@2
#   displayName: GOB-CoreBuildPush
#   inputs:
#     containerRegistry: 'Container Registry - piosupportacr'
#     repository: 'GOB-Core'
#     command: 'buildAndPush'
#     # arguments: --build-arg VERSION_TAG='$(VERSION_TAG)'
#     Dockerfile: 'GOB-Core/src/Dockerfile'
#     tags: |
#       $(Build.BuildId)-$(timeStamp)
#       latest

# - task: Docker@2
#   displayName: GOB-DistributeBuildPush
#   inputs:
#     containerRegistry: 'Container Registry - piosupportacr'
#     repository: 'GOB-Distribute'
#     command: 'buildAndPush'
#     # arguments: --build-arg VERSION_TAG='$(VERSION_TAG)'
#     Dockerfile: 'GOB-Distribute/src/Dockerfile'
#     tags: |
#       $(Build.BuildId)-$(timeStamp)
#       latest

# - task: Docker@2
#   displayName: GOB-ExportBuildPush
#   inputs:
#     containerRegistry: 'Container Registry - piosupportacr'
#     repository: 'GOB-Export'
#     command: 'buildAndPush'
#     # arguments: --build-arg VERSION_TAG='$(VERSION_TAG)'
#     Dockerfile: 'GOB-Export/src/Dockerfile'
#     tags: |
#       $(Build.BuildId)-$(timeStamp)
#       latest

# - task: Docker@2
#   displayName: GOB-ImportBuildPush
#   inputs:
#     containerRegistry: 'Container Registry - piosupportacr'
#     repository: 'GOB-Import'
#     command: 'buildAndPush'
#     # arguments: --build-arg VERSION_TAG='$(VERSION_TAG)'
#     Dockerfile: 'GOB-Import/src/Dockerfile'
#     tags: |
#       $(Build.BuildId)-$(timeStamp)
#       latest

# - task: Docker@2
#   displayName: GOB-InfraBuildPush
#   inputs:
#     containerRegistry: 'Container Registry - piosupportacr'
#     repository: 'GOB-Infra'
#     command: 'buildAndPush'
#     # arguments: --build-arg VERSION_TAG='$(VERSION_TAG)'
#     Dockerfile: 'GOB-Infra/src/Dockerfile'
#     tags: |
#       $(Build.BuildId)-$(timeStamp)
#       latest
